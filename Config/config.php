<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 要求图形验证码的频率，包括用户注册、用户登录、找回密码等涉及用户安全敏感操作
    |--------------------------------------------------------------------------
    | always: 总是需要，3times: 3 次尝试失败后需要
    */
    'captcha_frequency' => 'always',

    /*
    |--------------------------------------------------------------------------
    | 一天发送验证码的最大次数
    |--------------------------------------------------------------------------
    | 包括邮箱验证码和短信验证码
    */
    'send_code_max_times' => 5,

    /*
    |--------------------------------------------------------------------------
    | 定义注册方式，可以详细配置每一种登录方式是否需要发送验证码，
    | 或图形验证码验证，同时可以配置图形验证和短信验证码的频率限制。
    | 图形验证是默认需要的，只是定义什么时候出现。
    |--------------------------------------------------------------------------
    |
    | 支持 3 种注册方式，邮箱验证，手机验证和用户名。
    |
    */
    'register_channels' => ['email', 'mobile', 'username'],

    /*
    |--------------------------------------------------------------------------
    | 定义邮箱注册时是否需要验证邮箱，验证邮箱的类型有：邮箱验证码：code 或 注册链接：link
    |--------------------------------------------------------------------------
    |
    | 支持 4 种登录方式，邮箱和密码、手机和密码、用户名和密码、以及手机和短信验证码。
    |
    */
    'register_email_auth' => false,

    'register_email_auth_type' => 'code',  // link

    /*
    |--------------------------------------------------------------------------
    | 定义手机注册时，是否需要手机号验证
    |--------------------------------------------------------------------------
    */
    'register_mobile_auth' => false,

    /*
    |--------------------------------------------------------------------------
    | 定义登录方式
    |--------------------------------------------------------------------------
    |
    | 支持 4 种登录方式，邮箱和密码、手机和密码、用户名和密码、以及手机和短信验证码。
    |
    */
    'login_channels' => ['email', 'mobile', 'username', 'sms'],

    /*
    |--------------------------------------------------------------------------
    | 定义用手机登录时，手机号是否要求已验证
    |--------------------------------------------------------------------------
    */
    'login_mobile_auth' => true,

    /*
    |--------------------------------------------------------------------------
    | 定义用邮箱登录时，邮箱是否要求已验证
    |--------------------------------------------------------------------------
    */
    'login_email_auth' => false,

    /*
    |--------------------------------------------------------------------------
    | 定义找回密码的方式
    |--------------------------------------------------------------------------
    |
    | 支持 2 种找回密码方式，邮箱链接，短信验证码。
    |
    */
    'find_password_channels' => ['email', 'mobile'],

    /*
    |--------------------------------------------------------------------------
    | 重置密码的跳转链接
    |--------------------------------------------------------------------------
    */
    'reset_password_redirect_link' => 'http://app.kong72.com/#/resetpass',

    /*
    |--------------------------------------------------------------------------
    |更换邮箱跳转链接
    |--------------------------------------------------------------------------
    */
    'change_email_redirect_link' => 'http://app.kong72.com/#/change-email',
];
